# macOS
.DS_Store

# editor cruft
*.swp
*~
.idea/
.vscode/

# local backups the installer writes
*.bak
*.bak-statusline

# Claude Code project-level working folders (settings, worktrees, local state)
.claude/

# local-only project guide — never pushed to the remote
CLAUDE.md
