# Config-file path honored: point at a committed sample conf ($ROOT is exported
# by run.sh). No matching env var is set, so the file's EFFORT_HIGH=cfg wins over
# the built-in ▲ default → golden shows "high cfg".
MOUNTAINLABS_STATUSLINE_CONFIG="$ROOT/tests/fixtures/config-sample.conf"
