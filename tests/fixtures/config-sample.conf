# Sample config used by the #0007 override tests (not a fixture itself — the
# fixture loop only globs *.json). Overrides an effort glyph (golden-visible,
# ANSI is stripped) and a threshold (parsed but color-only, so not golden-visible;
# it proves a threshold override is read without breaking the render).
EFFORT_HIGH=cfg
CTX_RED_TOK=250000
