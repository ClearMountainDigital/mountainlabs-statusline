# Precedence env > file: the sample conf sets EFFORT_HIGH=cfg, but an env var
# also sets it to "env". Env must win → golden shows "high env", proving the
# snapshot/re-apply in statusline.sh keeps env above the config file.
MOUNTAINLABS_STATUSLINE_CONFIG="$ROOT/tests/fixtures/config-sample.conf"
EFFORT_HIGH=env
